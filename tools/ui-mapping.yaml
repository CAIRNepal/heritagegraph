# Optional overrides: LinkML slot range name → UI field type (004-yaml-driven-schema).
# MVP mapping is implemented in heritage_graph/apps/cidoc_data/ontology_builder.py (_range_to_field_type).
# Example:
# overrides:
#   custom_slot_key: textarea

overrides: {}
