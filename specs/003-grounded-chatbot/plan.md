# Implementation Plan: Grounded frontend chatbot

**Branch**: `003-grounded-chatbot` | **Date**: 2026-04-18 | **Spec**: `specs/003-grounded-chatbot/spec.md`  
**Input**: Feature specification from `specs/003-grounded-chatbot/spec.md`

## Summary

**Current state (repo):** The in-product **chat** in `heritage_graph_ui` and `heritage_graph_landing` **POSTs** to `POST /api/v1/assistant/chat/` (see `lib/chat/assistantClient.ts`). The backend runs **grounding** + **retrieval** and calls **OpenRouter** with **tiered** models. `dummyResponses.ts` may remain for **optional** local/offline toggles; it is not the default production path.

**Target state (as implemented):** A **versioned** backend **chat completion** endpoint that (1) loads **curated** site/mission copy from **grounding files** under `apps/assistant/grounding/`, (2) **retrieves** public CIDOC / discovery rows via the same patterns as `apps/cidoc_data/views.py`, and (3) calls an **LLM** through **OpenRouter** (OpenAI-compatible `chat.completions` from `apps/assistant/services/openrouter.py`) with a **strict** “answer only from context” system message. **Cost tiers** select `OPENROUTER_MODEL_*` per request using **server-side** heuristics in `apps/assistant/services/routing.py`. **OCR/vision** in `document_processing` still uses **direct Anthropic**, not OpenRouter. Both Next.js apps call the API (see `assistantClient.ts`), preserving **loading**, **order**, and **error** UX.

**Not in v1 (unless tasks expand):** Vector DB RAG, streaming, persistent chat history in PostgreSQL, automatic HTML scraping of every React page.

## Technical Context

**Language/Version**: Python 3.13 (Django + DRF), TypeScript (Next.js 15, React 19)  
**Primary Dependencies**: DRF `@api_view` for `POST /api/v1/assistant/chat/`; **`openai`** Python package (OpenAI-compatible client → OpenRouter); **Anthropic** remains for **OCR/vision** only; Next.js `fetch` + `apiFetchJson` / `getPublicApiUrl` in `heritage_graph_ui`  
**Storage**: No new database tables in v1 (ephemeral messages); **grounding** text files in the backend tree; **PostgreSQL** for retrieved CIDOC models as today  
**Testing**: `python manage.py test` for the new app’s permissions and view edge cases; manual checks per `quickstart.md`  
**Target Platform**: Web (Docker/Traefik per repo)  
**Project Type**: Web application (Django API + Next.js App Router UI; second landing app mirrors chat)

**Performance Goals (align with `spec.md` SC-002):** User-visible **round-trip** under **30s p95** for typical prompts, or a **clear** timeout/error. Avoid unbounded model context: **cap** retrieved rows and string lengths.

**Constraints (constitution + spec):**

- No committed secrets; new server env (model key) **documented in `.env.example`**; frontend `NEXT_PUBLIC_*` only, **no** hardcoded `http://localhost` in new production paths (dev fallback in `getPublicApiUrl` is existing behavior).
- Protected data: use **`Authorization: Bearer <accessToken>`** from NextAuth when the user is signed in; for **v1** retrieval, **default** to **public-safe** data unless a clear, audited rule enables more with auth.
- **Ruff** + **TS** quality gates for touched code.

**Scale/Scope (initial):**

- New `POST` handler under `api/v1/assistant/`.
- **Both** `heritage_graph_ui` and `heritage_graph_landing` chat panels.
- **Grounding files** for About/mission/usage, plus **search**-driven context for **heritage** Q&A.
- **Optional** `nav` in JSON response, **server allow-listed** to prevent open redirects.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

**Pre-Phase-0: PASS**

- **Secrets & config:** Model key stays server-side; public API URL remains env-driven on the client.
- **Conventions:** New code in a small Django app (e.g. `heritage_graph/apps/assistant/`) with DRF, explicit permissions; frontend named exports, no direct shadcn edits, Tailwind via tokens.
- **Auth contract:** When session exists, pass Bearer; backend validates JWT per existing DRF/Next patterns.
- **Quality gates:** ruff + `next` build for touched code.
- **Deployability:** Add URL include in `heritage_graph/urls.py` under `api/v1/assistant/`; no breaking change to existing data APIs; if behavior is “first public LLM in prod,” note rollout in release notes (not a constitution waiver).

**Post-Phase-1: PASS** — design artifacts: `research.md` (R-001–R-007), `data-model.md`, `contracts/openapi-assistant-chat.v1.yaml`, `quickstart.md`. New URL namespace only; no migration required for v1.

## Project Structure

### Documentation (this feature)

```text
specs/003-grounded-chatbot/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── openapi-assistant-chat.v1.yaml
└── (generated by /speckit.tasks) tasks.md
```

### Source Code (repository root)

```text
heritage_graph/
├── apps/assistant/                    # views, grounding/, services (retrieval + OpenRouter + routing)
│   ├── __init__.py
│   ├── apps.py
│   ├── views.py
│   ├── services/
│   │   ├── chat_completion.py        # system prompt, tier log, openrouter call
│   │   ├── openrouter.py            # OpenAI client → OpenRouter
│   │   ├── routing.py              # select_tier (server-only); model_id_for_tier
│   │   └── retrieval.py            # uses cidoc_data search/discovery query helpers
│   ├── nav_allowlist.py
│   ├── grounding/                  # site copy .md; mission / product / usage
│   └── tests/
├── apps/cidoc_data/
│   └── views.py                    # optional: factor universal_search / discovery to shared fn
heritage_graph/
└── urls.py                         # path("api/v1/assistant/", include("apps.assistant.urls"))
heritage_graph_ui/
├── src/
│   ├── lib/chat/dummyResponses.ts  # keep for optional dev-only fallback, or remove when unused
│   ├── lib/chat/assistantClient.ts  # NEW: call POST /api/v1/assistant/chat/
│   └── components/chat/ChatPanel.tsx  # use assistantClient, abort + timeout, errors
heritage_graph_landing/
└── src/
    ├── lib/chat/assistantClient.ts  # same pattern as UI (or documented copy)
    └── components/chat/ChatPanel.tsx
```

**Structure Decision:** New **`assistant`** Django app to isolate LLM/grounding and avoid bloating `cidoc_data` / `heritage_data`. Reuse **cidoc** query logic by **importing** or extracting helpers—avoid HTTP self-calls to the same process.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| (none) | | |

**Operational note (not a waiver):** The assistant path uses **OpenRouter** with **tiered** models; monitor **per-tier** cost and rate limits in the OpenRouter dashboard. Cap request size; document in [`AGENTS.md`](../../AGENTS.md) and root `.env.example` (`OPENROUTER_*`, `ASSISTANT_TIER_*`).

## Phase 0: Research (see `research.md`)

- Resolved: **Django** vs BFF, **grounding** strategy (file + search retrieval), **OpenRouter** + **tiered** models for the assistant (OCR still **Anthropic**), **public vs authed** behavior, **dual** frontend, **nav** allow list.
- No outstanding `[NEEDS CLARIFICATION]` in technical context; details are in `research.md` R-001–R-007.

## Phase 1: Design outputs

- `research.md` — decisions and alternatives.
- `data-model.md` — DTOs and client message lifecycle (no new ORM).
- `contracts/openapi-assistant-chat.v1.yaml` — public contract for `POST` chat.
- `quickstart.md` — manual and reviewer rubric for SC-001/SC-003.

**Agent context:** Updated in `.cursor/rules/specify-rules.mdc` to reference this plan and `quickstart.md` for this feature.

## Phase 2+

- Run **`/speckit.tasks`** to produce `tasks.md` (implementation breakdown: backend app, url wiring, retrieval refactor, two frontends, `.env.example`, tests, smoke).
- **Not produced by this command:** `tasks.md`.
