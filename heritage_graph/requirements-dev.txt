# Local / CI tooling (not required for lean production image unless you run generators in Docker).
# Install: pip install -r heritage_graph/requirements-dev.txt
linkml>=1.8.0,<2
linkml-runtime>=1.8.0,<2
