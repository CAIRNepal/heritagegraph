# ================================================================
# OCR and Document Processing Dependencies
# ================================================================
# This file contains heavy dependencies for document processing:
# - PyTorch and transformers for TrOCR and NER
# - Tesseract OCR engine
# - EasyOCR for multi-script fallback
# - PDF and image processing libraries
# - Anthropic SDK for Claude Vision rescue
#
# Deploy as a separate service (ocr-worker) to keep main backend lightweight.
# Use: pip install -r requirements-ocr.txt
#
# PRODUCTION NOTE: This creates a ~3GB image. Consider:
# 1. Separate OCR worker container from main backend
# 2. Only enable OCR on specific deployments (not all instances)
# 3. Cache model downloads in Docker volume
# ================================================================

# OCR Engines
pytesseract==0.3.13
easyocr==1.7.0
surya-ocr==0.1.7

# Handwritten Text Recognition (HTR)
# TrOCR via transformers
transformers==4.45.0
torch==2.4.0
torchvision==0.19.0

# NER and structured extraction
instructor==1.3.4
spacy==3.7.2

# Document processing
pdf2image==1.16.3
pdfplumber==0.11.0
Pillow==12.1.1  # Already in main requirements, but ensure

# Claude Vision for rescue OCR
anthropic==0.31.0

# Utilities
numpy==1.24.3
opencv-python==4.8.1.78
